import Store from '@/store/store'

export default {
    data () {
        return {
            title: '登录'
        }
    },
    mounted () {
        // console.log(this.$store.state.userInfo.userName)
    }
}
